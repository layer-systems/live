apiVersion: v1
kind: Service
metadata:
  name: {{ include "nostrlive.fullname" . }}
  labels:
    {{- include "nostrlive.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nostrlive.selectorLabels" . | nindent 4 }}
